# 固定的共享前端制品

这些包来自已验收的 WWW 前端构建组合；固定内容编辑器及它依赖的 UI、HTTP、会话和媒体模块，保证独立构建不会回到旧接口。Tiptap 使用同一组补丁版本。

内容组件来源：Foundation `flightdeck/work/2026-09-09-editor-import/index.md`。这是本地构建并验收的制品，不代表 npm/GitHub 正式发布。正式发布同一内容后，可把依赖切换为对应制品地址。当前包与锁文件必须一起保留。
